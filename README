Pretty simple set of scripts. fake dns and fake arp can be used on their own or together with 'fake_net'.

run ./fake_dns2 --help
Usage: fake_dns2.py [options]

Options:
  -h, --help            show this help message and exit
  -i IFACE, --interface=IFACE
                        Interface to listen and send pkts on


Only argument required is the interface to listen/send packets one
